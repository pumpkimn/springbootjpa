server:
  port: 8086
  servlet:
    context-path:
spring:
#配置mybatis
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/employee?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
  jpa:
    show-sql: true
     # 第一次运行create  改为update ֱ在改Ϊ none
    hibernate:
      ddl-auto: none
